# タスクリスト — Version 1.0.1 ヘルプオプション

## タスク一覧

### 1. Cargo.toml 更新
- [ ] バージョンを `1.0.1` に更新する

### 2. `src/main.rs` 更新
- [ ] `print_global_help()` 関数を実装する
- [ ] `print_command_help(cmd: &str)` 関数を実装する
- [ ] `is_help(args: &[String])` ヘルパー関数を実装する
- [ ] `run()` にグローバルヘルプ検出処理を追加する（`args[1]` が `-h`/`--help`）
- [ ] 各サブコマンド処理にサブコマンド別ヘルプ検出を追加する

### 3. 品質チェック
- [ ] `cargo test` が全て通過することを確認する
- [ ] `cargo clippy` でワーニングがないことを確認する
- [ ] `cargo fmt` でフォーマットを統一する
- [ ] 受け入れ基準を手動で確認する

## 完了条件

- `cargo test` が全テスト通過
- `-h` / `--help` でグローバルヘルプが表示される
- 各サブコマンドで `--help` が動作する
- `cargo clippy` でワーニングなし
